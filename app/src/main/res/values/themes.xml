<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ProductExplorer" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.App.Starting" parent="Base.Theme.ProductExplorer">
        <!-- CRITICAL: Set the background to a drawable that looks like your splash screen -->
        <item name="android:windowBackground">@drawable/splash_background</item>
        <!-- Optional: For Android 12+ you can configure the new Splash Screen API properties -->
<!--        <item name="postSplashScreenTheme">@style/Theme.ProductExplorer</item>-->
    </style>

<!--    <style name="Theme.ProductExplorer" parent="Base.Theme.ProductExplorer" />-->
</resources>